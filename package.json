{
  "name": "odyssey",
  "version": "1.0.0",
  "description": "Odyssey - Transform Your Commute into interactive journeys",
  "main": "js/main.js",
  "scripts": {
    "start": "serve",
    "build": "echo 'No build step required' && exit 0",
    "test": "echo 'No tests configured'",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "firebase": "^9.22.0",
    "@turf/turf": "^6.5.0",
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "firebase-tools": "^12.0.0"
  }
} 